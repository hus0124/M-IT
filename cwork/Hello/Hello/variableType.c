/*
변수타입
-정수형 : char( 1byte), short(2 byte), int(4 byte), long(4 byte), long long(8 byte)
 -> 사용하는 CPU의 비트수에 따라서 크기가 다를 수 있다.
 -> 8 bit 컴퓨터에서는 int 는 16 bit(2 byte) 이고
 -> 32 bit 컴퓨터에서는 int 는 32 bit(4 byte) 이다. 
 C 언어는 하드웨어에 의존하는 언어이다. CPU가 다르면 bit 수에 따라 다르므로 컴파일을 다시해야 한다. 

 -실수형 : float(4 byte), double(8 byte)
 -> 실수형 데이터 이므로 소수점 표현 가능하다.
*/


#include <stdio.h>

